{
  "name": "p-contract-mobile",
  "version": "1.0.0",
  "description": "create project",
  "private": true,
  "scripts": {
    "start": "rstool --server --port 9001",
    "dev": "rstool --env dev --hash --extract --cdn",
    "uat1": "rstool --env uat1 --hash --extract --cdn",
    "stg": "rstool --env stg --hash --extract --cdn",
    "prd": "rstool --env prd --hash --extract --cdn ",
    "lint": "eslint --ext .js,.vue src",
    "lint-fix": "eslint --fix --ext .js,.vue src"
  },
  "dependencies": {
    "axios": "0.16.2",
    "lib-flexible": "0.3.2",
    "vue": "2.4.2",
    "vue-router": "2.7.0"
  },
  "devDependencies": {
    "babel-plugin-component": "0.10.1",
    "element-ui": "1.4.12",
    "eslint-config-rs": "0.1.13",
    "lodash-webpack-plugin": "^0.11.4",
    "mint-ui": "^2.2.12",
    "rstool": "^0.2.6"
  },
  "publishConfig": {
    "registry": "http://npm.corp.rs.com/"
  }
}
